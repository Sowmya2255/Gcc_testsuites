
This sample.exp file made to --SEARCHES FOR A PARTICULAR TEXT IS PRESENT OR NOT IN THE GIVEN input contains testcases for PASS, FAIL and X_Fail conditions Here what exactly the code does: sets the status to run on host machine printing the text we want and outputs the status into status file Uses lindex to list the index and prints the output where o indicates exit code and 1 represents the output (what we give in the echo) the regexp is a variable which searches for the variable we are searching for

I used 3 testcases searching for PASS, FAIL and XFAIL condition.
gcc.log and gcc.sum files has been included 
